<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="listadetarefas.cursoandroid.com.listadetarefas.MainActivity"
    android:background="#575a78"
    android:orientation="vertical">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="42dp"
        android:text="Adicionar"
        android:id="@+id/botaoAdicionarId"
        android:layout_alignBaseline="@+id/textoId"
        android:layout_alignBottom="@+id/textoId"
        android:layout_alignEnd="@+id/listViewId"
        android:layout_gravity="right" />

    <EditText
        android:layout_width="258dp"
        android:layout_height="wrap_content"
        android:id="@+id/textoId"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/botaoAdicionarId"
        android:background="#ffffff"
        android:padding="5dp" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listViewId"
        android:layout_below="@+id/botaoAdicionarId"
        android:layout_alignParentStart="true"
        android:background="#31000000" />
</RelativeLayout>
